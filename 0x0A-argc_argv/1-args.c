#include <stdio.h>
/**
 * main - entry point
 * @argc: length of array
 * @argv: the array
 *
 * Return: length of array
 */

int main(int argc, char __attribute__((unused)) *argv[])
{
	printf("%i\n", argc - 1);
	return (0);
}

#include "main.h"
/**
 * _isupper - check if c is uppercase
 * @c: the character to check
 * Return: 0 if uppercase 1 if lowercase
 */
int _isupper(int c)
{
	if (c < 'a')
		return (1);

	return (0);

}

#include "main.h"
/**
 * _strcat - concatinate 2 strings
 * @dest: first string
 * @src: second string
 * Return: the result of 2 added strings
 */
char *_strcat(char *dest, char *src)
{
	_putchar(*dest);
	_putchar(*src);
	_putchar('\n');
}
